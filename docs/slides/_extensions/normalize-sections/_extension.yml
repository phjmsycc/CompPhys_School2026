title: Normalize non-slide sections
version: 1.0.0
quarto-required: ">=1.8.0"
contributes:
  revealjs-plugins:
    - name: NormalizeNonSlideSections
      register: false
      script:
        - normalize-sections.js
